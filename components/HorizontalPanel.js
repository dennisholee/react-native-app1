import React, { Component } from "react";
import { Text } from "react-native";

class HorizontalPanel extends Component {
  render() {
    return (

      <Text style={{flexDirection: "row"}}>Horizontal Panel</Text>
    )
  }
}
